# Copia a .streamlit/secrets.toml y rellena. NUNCA subas secrets.toml a git.
# El service-role key solo vive del lado servidor (Streamlit corre en servidor).

SUPABASE_URL = "https://xxxxxxxxxxxx.supabase.co"
SUPABASE_ANON_KEY = "eyJhbGciOi..."          # segura para el cliente (RLS la protege)
SUPABASE_SERVICE_ROLE_KEY = "eyJhbGciOi..."  # bypassa RLS — ingesta/recálculo
SUPABASE_STORAGE_BUCKET = "raw-campaigns"
